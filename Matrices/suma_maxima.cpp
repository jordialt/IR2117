#include <iostream>
#include <Eigen/Dense>

using Eigen::MatriXd;

int main{
    int rows,cols;
    double suma=0;
    std::cout<<"Enter matrix size: "<<std::endl;
    std::cin>>rows;
    cols=rows;
    MatrixXd m(rows,cols);
    std::cout<<suma<<endl;
    return 0;
    
}

